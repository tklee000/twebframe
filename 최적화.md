# TWebFrame 최적화 검토

작성일: 2026-09-21

## 1. 검토 범위

다음 실행 경로를 기준으로 소스를 검토했다.

- DOM 및 JavaScript mutation
- CSS selector/cascade 계산
- LayoutBox 생성 및 geometry 계산
- Direct2D/DirectWrite 페인트
- 마우스·키보드·스크롤·커서 갱신
- 접근성 트리 및 live region 갱신

현재 문서에도 dirty-subtree layout과 incremental rendering은 아직 목표가 아니라고 명시되어 있다(`docs/ARCHITECTURE.md:42`). 따라서 아래 항목은 현재 구조에서 가장 큰 비용이 발생하는 지점을 중심으로 정리했다.

## 2. 핵심 결론

가장 우선적으로 개선할 부분은 다음 두 가지다.

1. DOM 또는 스타일 변경 시 전체 레이아웃 트리를 재생성하는 구조
2. 작은 변경에도 전체 오프스크린 프레임을 다시 페인트하는 구조

현재 주요 흐름은 다음과 같다.

```text
DOM/스타일 변경
  -> mutation sink
  -> layoutDirty = true
  -> WM_PAINT
  -> Layout::Layout()
  -> 전체 LayoutBox 트리 재생성
  -> 전체 geometry/layout 계산
  -> 전체 프레임 페인트 및 back buffer 복사
```

## 3. 우선순위 요약

| 우선순위 | 영역 | 주요 근거 | 예상 영향 |
|---|---|---|---|
| P1 | 레이아웃 무효화 | `View.cpp:342`, `Layout.cpp:3106` | 대형 DOM의 동적 갱신, 입력, 애니메이션 지연 |
| P1 | 부분 페인트 | `View.cpp:714`, `Layout.cpp:3825` | 커서·스크롤·hover에도 전체 화면 렌더링 |
| P1 | 접근성 mutation 검사 | `View.cpp:681` | mutation마다 전체 DOM 검색 및 텍스트 생성 |
| P1 | CSS selector/cascade | `CSS.cpp:442`, `CSS.cpp:807` | 노드 수와 CSS 규칙 수에 따른 비용 증가 |
| P2 | DOM query/index | `DOM.cpp:504`, `DOM.cpp:554`, `DOM.cpp:614` | querySelector 및 ID 변경 시 전체 트리 탐색 |
| P2 | Direct2D 리소스 | `Layout.cpp:3844`, `Layout.cpp:3968` | 많은 박스에서 COM 리소스 생성 반복 |
| P2 | JavaScript 이벤트/VM | `JavaScript.cpp:2044`, `JavaScript.cpp:2511` | 고빈도 이벤트와 inline handler에서 비용 증가 |
| P3 | 문자열·컨테이너 할당 | `DOM.cpp:409`, `DOM.cpp:430`, `Layout.cpp:2941` | 전체 레이아웃과 selector 계산의 누적 비용 |

## 4. 상세 검토

### P1-1. 전체 레이아웃 재생성

`View::Rebuild()`는 viewport-only 변경이 아닌 경우 항상 `layout.Layout()`을 호출한다(`TWebFrame/src/View.cpp:446`).

`LayoutEngine::Layout()`은 다음 작업을 모두 다시 수행한다.

- 전체 `LayoutBox` 트리 생성
- 노드별 computed style 계산
- 전체 geometry 계산
- scroll extent 계산
- paint order 및 stacking context 계산

관련 코드:

- `TWebFrame/src/Layout.cpp:2906` — 노드별 LayoutBox 및 style 생성
- `TWebFrame/src/Layout.cpp:3106` — 전체 Layout 진입점
- `TWebFrame/src/Layout.cpp:3157` — 전체 geometry 계산
- `TWebFrame/src/Layout.cpp:3180` — paint/hit-test 메타데이터 재생성

모든 JavaScript mutation은 `layoutDirty=true`로 연결된다(`TWebFrame/src/View.cpp:342`). 따라서 다음 변경도 전체 layout으로 처리된다.

- `style.color`와 같은 paint-only 속성
- `scrollTop`, `scrollLeft`
- focus 상태 변경
- 일부 접근성 상태 변경

`LayoutEngine::Restyle()`과 `HasLayoutStyleChange()`가 이미 존재하지만, 현재 일반 mutation 경로에는 충분히 활용되지 않고 hover 처리 중심으로 사용된다(`TWebFrame/src/View.cpp:961-967`).

#### 권장 개선

무효화 수준을 다음처럼 분리한다.

- `PaintOnly`: 색상, opacity, outline, text decoration
- `ScrollOnly`: scrollTop, scrollLeft, scrollbar thumb
- `Restyle`: class, pseudo-state, 상속 style 변경
- `Layout`: 크기·위치·font·display·margin 변경
- `TreeRebuild`: child 삽입·삭제·innerHTML 변경

1차 단계에서는 paint-only와 scroll-only를 full layout에서 분리하고, 2차 단계에서 dirty subtree layout을 도입하는 것이 안전하다.

### P1-2. 전체 오프스크린 페인트

`View::Paint()`는 `PAINTSTRUCT.rcPaint`를 사용하지 않고 매번 전체 back buffer를 다음 순서로 갱신한다(`TWebFrame/src/View.cpp:714-746`).

- 전체 surface clear
- 전체 layout tree paint
- back buffer bitmap 생성/조회
- 전체 bitmap을 HWND render target에 복사

`LayoutEngine::Paint()`도 viewport 전체를 clip으로 전달한다(`TWebFrame/src/Layout.cpp:3825-3833`). 수직 정렬된 children에 대한 culling은 있지만, dirty rectangle 기반의 retained rendering은 아니다.

특히 다음 동작이 전체 페인트를 유발한다.

- caret blink
- mouse wheel scroll
- scrollbar drag
- hover 변경
- select popup hover
- text selection 변경

#### 권장 개선

1. `BeginPaint()`의 `rcPaint`를 dirty rectangle로 보존한다.
2. `LayoutEngine::Paint()`에 dirty clip을 전달한다.
3. back buffer 전체를 clear하지 않고 dirty 영역만 갱신한다.
4. caret, selection, popup은 별도 overlay 영역으로 관리한다.
5. scroll 시 가능하면 기존 bitmap을 이동시키고 노출된 영역만 다시 그린다.

### P1-3. 접근성 mutation 검사

접근성 객체는 초기화 시 항상 생성된다(`TWebFrame/src/View.cpp:657-658`). mutation마다 `NotifyAccessibilityMutation()`이 호출되고(`TWebFrame/src/View.cpp:342`), 내부에서 다음 작업을 한다.

- `[aria-live]` selector로 전체 DOM 순회
- 각 live region의 `InnerText()` 재계산
- 이전 문자열과 비교

관련 코드: `TWebFrame/src/View.cpp:681-687`.

`aria-live`가 없는 일반 문서에서도 이 검색은 실행된다.

#### 권장 개선

- live region이 존재하는 경우에만 검사
- `aria-live` 노드 목록을 별도 index로 유지
- mutation 대상이 live region 또는 그 조상인 경우에만 텍스트 재계산
- UI Automation consumer가 없는 경우 live event 계산 지연

### P1-4. CSS selector 및 cascade 계산

`StyleSheet::Compute()`는 노드마다 다음 작업을 반복한다.

- 후보 규칙 vector 생성
- media 조건 확인
- `Document::MatchesSelector()` 호출
- selector 문자열 파싱·복사·삭제
- cascade winner map 구성
- custom property map 복사

관련 코드:

- `TWebFrame/src/CSS.cpp:442-457` — 후보 규칙 수집
- `TWebFrame/src/CSS.cpp:557-624` — inherited/custom property 처리
- `TWebFrame/src/CSS.cpp:807-821` — 규칙 매칭 및 선언 적용
- `TWebFrame/src/DOM.cpp:523-551` — selector ancestor/sibling 탐색

특히 `:has()`, `:nth-child()`, sibling combinator는 selector 매칭 때마다 추가적인 subtree 또는 sibling 탐색을 발생시킨다.

#### 권장 개선

- selector를 문자열이 아닌 사전 파싱된 AST/토큰 구조로 저장
- tag/id/class를 intern하여 문자열 생성 최소화
- viewport 변경 시 활성 media rule을 미리 계산
- node별 style state와 structural revision 기반으로 match 결과 캐시
- `nth-child` 및 `nth-of-type` 인덱스 캐시
- inherited custom property를 매 노드마다 복사하지 않고 부모 chain으로 참조

### P2-1. DOM query 및 index

`querySelector()`는 실제로 `QuerySelectorAll()`을 호출한 뒤 첫 결과만 반환한다(`TWebFrame/src/DOM.cpp:554-558`). 따라서 첫 매칭 후 종료할 수 있는 상황에서도 전체 DOM을 순회하고 결과 vector를 만든다.

또한 `Document::QuerySelectorAll()`은 selector마다 전체 tree walk를 수행한다(`TWebFrame/src/DOM.cpp:560-584`).

ID index도 mutation 시 전체 문서를 다시 순회한다(`TWebFrame/src/DOM.cpp:614-620`). `appendChild`나 `innerHTML` 변경 시 새 subtree에 ID가 하나만 있어도 전체 `Reindex()`가 실행될 수 있다.

#### 권장 개선

- `QuerySelector()` 전용 조기 종료 순회 구현
- tag/id/class별 DOM candidate index 추가
- ID 변경 시 이전/새 ID만 갱신
- subtree 삽입 시 삽입 subtree만 index에 반영
- `HasClass()`용 tokenized class cache 유지

### P2-2. Direct2D 리소스 생성 반복

`PaintBox()`는 박스와 속성에 따라 `CreateSolidColorBrush()`를 반복 호출한다(`TWebFrame/src/Layout.cpp:3844-3960`). select arrow와 scrollbar arrow의 path geometry도 페인트마다 다시 생성된다(`TWebFrame/src/Layout.cpp:3968-3975`, `4013-4028`).

#### 권장 개선

- render target별 색상 brush cache
- select arrow와 scrollbar arrow geometry cache
- SVG geometry cache
- render target 재생성 시에만 캐시 폐기
- opacity layer는 필요한 경우에만 생성

DirectWrite text layout은 이미 `LayoutBox` 내부에 캐시되고 있으므로, 우선순위는 brush/path 쪽이 더 높다.

### P2-3. JavaScript 이벤트 및 VM

inline event handler는 이벤트 발생 때마다 소스를 다시 compile한다(`TWebFrame/src/JavaScript.cpp:2499-2515`). `onclick`, `onmousemove`, `oninput`이 많은 페이지에서는 불필요한 parser/compiler 비용이 반복된다.

event listener dispatch도 listener vector를 복사한 뒤 실제 map에서 다시 검색한다(`TWebFrame/src/JavaScript.cpp:2038-2050`). listener 변경 안정성을 위한 동작이지만, 고빈도 pointer/mouse 이벤트에서는 비용이 누적된다.

VM 내부에서도 property/reference 접근 때 `shared_ptr` 기반 Reference를 생성한다(`TWebFrame/src/JavaScript.cpp:2224-2227`). 일반 함수 호출마다 Environment와 arguments array도 생성된다(`TWebFrame/src/JavaScript.cpp:2103-2113`).

#### 권장 개선

- inline handler를 attribute별로 compile cache
- listener snapshot을 변경 버전 기반으로 재사용
- hot property path에서 Reference allocation 감소
- 짧은 함수 호출용 stack/local frame pool 검토
- animation frame callback에서 opcode 수와 allocation 수 측정

### P3. 문자열 및 컨테이너 할당

다음 작업들은 개별 비용은 작지만 전체 layout과 selector 계산에서 반복된다.

- `Node::Attribute()` 호출마다 `ToLower()` 결과 생성(`DOM.cpp:409-412`)
- `Node::HasClass()` 호출마다 `wistringstream` 생성(`DOM.cpp:430-435`)
- `LayoutEngine::Build()`에서 노드마다 `hasFollowingContent` vector 생성(`Layout.cpp:2941`)
- `InnerText()`의 재귀 문자열 연결(`DOM.cpp:456-461`)
- `FinalizeScroll()`에서 scrollable ancestor마다 descendant extent 재탐색(`Layout.cpp:3253-3278`)

#### 권장 개선

- lowercase attribute name overload 또는 interned attribute key 사용
- class token cache 및 revision 도입
- `InnerText()`에 reserve 또는 append buffer 사용
- scroll extent를 bottom-up layout 단계에서 함께 계산
- 반복 vector는 scratch arena 또는 재사용 buffer 사용

## 5. 구현 권장 순서

### 1단계: 계측

다음 항목을 frame 단위로 측정한다.

- `Rebuild()` 호출 횟수 및 소요 시간
- `Layout::Build()` 노드 수 및 소요 시간
- `StyleSheet::Compute()` 호출 수
- selector match 횟수
- paint box 수
- brush/path/text layout 생성 수
- accessibility live-region scan 시간
- JavaScript opcode 수 및 allocation 수

### 2단계: 위험도가 낮은 개선

- `querySelector()` 조기 종료
- live region scan 조건화
- scroll-only mutation 분리
- paint-only style mutation에 `Restyle()` 사용
- class token 및 attribute lowercase cache
- brush/path cache

### 3단계: 구조적 개선

- dirty subtree layout
- retained back buffer와 dirty rectangle paint
- DOM incremental index
- selector AST 및 match cache
- scroll extent bottom-up 계산

## 6. 성공 기준

최적화 후에는 다음 시나리오를 별도로 측정해야 한다.

1. 1만 개 이상의 list item에서 hover 이동
2. 긴 문서에서 wheel scroll
3. input text 입력 및 caret blink
4. `requestAnimationFrame()`에서 class/style 반복 변경
5. 큰 subtree의 `innerHTML` 갱신
6. `aria-live`가 없는 문서와 있는 문서의 mutation 비용 비교
7. CSS `:nth-child`, `:has`, sibling selector가 많은 문서

비교 지표는 평균 시간보다 p95/p99 frame time, layout 횟수, paint box 수, peak allocation을 함께 기록하는 것이 적절하다.

## 7. 현재 확인된 장점

현재 코드에도 다음 최적화는 이미 적용되어 있다.

- JavaScript mutation batch로 한 task의 mutation 알림을 합침
- viewport-only resize 시 기존 layout tree 재사용
- DirectWrite text layout cache
- paint/hit-test용 stacking order cache
- 수직 정렬 children에 대한 paint culling
- `getElementById()`용 ID index
- hover 변경 시 subtree restyle 경로

따라서 다음 최적화의 핵심은 새 캐시를 무조건 추가하는 것이 아니라, 변경 유형에 따라 layout·paint·accessibility 작업을 분리하는 것이다.

## 8. 검증 상태

- 최적화 반영 후 x64 Release 전체 빌드 완료(Visual Studio 2019 MSBuild)
- 최적화 반영 후 Release 테스트 실행 결과: `All TWebFrame tests passed`
- `TWebFrameTests.exe --benchmark`로 1만 노드·120 frame 계측 경로 추가
- 기준 커밋 `c795ae1`과 현재 작업 트리를 동일 머신·동일 Release 설정·동일 하네스로 비교

| 시나리오 | 기준 mean | 현재 mean | 기준 p95 | 현재 p95 | 해석 |
|---|---:|---:|---:|---:|---|
| initial load + paint | 875.784ms | 746.407ms | - | - | 약 14.8% 감소 |
| hover | 44.872ms | 45.234ms | 45.636ms | 46.819ms | 변화 없음(측정 잡음 범위) |
| style toggle | 624.344ms | 9.614ms | 660.672ms | 46.159ms | 평균 약 64.9배, p95 약 14.3배 개선 |
| wheel scroll | 45.464ms | 45.279ms | 46.323ms | 46.392ms | 변화 없음(측정 잡음 범위) |

현재 style toggle의 p50은 0.094ms다. 화면 밖 노드의 paint를 제거하고 style mutation에서 전체 layout을 피한 효과가 평균값에 반영됐다. 반면 hover와 wheel scroll은 두 버전 모두 약 45ms로 유지된다. 이는 현재 하드웨어에서 화면 갱신이 발생한 frame의 비용이 layout보다 오프스크린 D2D paint에 지배되고 있음을 보여 준다.

위 값은 단일 실행 결과이므로 절대 성능 회귀 판정에는 반복 실행과 분산 확인이 더 필요하다. 다만 style mutation의 개선 폭과 paint 병목의 위치를 판단하기에는 차이가 충분히 크다.

## 9. 구현 진행 상태

2026-09-22에 위험도가 낮고 독립적으로 검증할 수 있는 1차 최적화를 반영했다.

### 완료

- JavaScript mutation을 `Paint`, `Style`, `Layout`, `Tree` 단계로 분리
  - `scrollTop`, `scrollLeft`, `scroll()`, `scrollTo()`, `scrollBy()`는 레이아웃을 재생성하지 않음
  - paint-only DOM scroll은 LayoutBox에 적용된 scroll 위치와의 차이만 기존 subtree 좌표에 반영
  - class, inline style, attribute 변경은 기존 layout tree에 `Restyle()`을 적용하고 geometry 변경이 감지된 경우에만 전체 layout 수행
  - sibling combinator와 `:has()`가 있는 stylesheet는 정확성을 위해 전체 tree restyle 사용
- `PAINTSTRUCT.rcPaint`를 back buffer 및 layout paint clip에 전달
  - dirty 영역만 back buffer에서 지우고 다시 그림
  - HWND target에도 dirty 영역만 복사
  - caret blink는 caret 주변만 invalidate
  - 안전하게 계산 가능한 style/hover mutation은 이전·새 subtree 시각 범위만 invalidate
  - DOM scroll mutation은 해당 scroll container 범위만 invalidate
  - transform 등 시각 범위를 보수적으로 계산할 수 없는 경우 전체 invalidate로 fallback
- render target별 solid-color brush cache 추가(최대 512색, target 변경 시 자동 폐기)
- Direct2D path geometry 재사용
  - select 화살표와 수평·수직 scrollbar 화살표를 unit geometry로 한 번만 생성
  - SVG `path`, `polygon`, `polyline` geometry를 내용 기준으로 최대 256개 캐시
  - render target의 factory가 바뀌면 관련 geometry cache를 자동 폐기
- DOM ID index를 고유 ID/subtree 변경에 대해 증분 갱신
  - 삽입, 이동, 삭제, `id` 변경은 전체 `Reindex()` 없이 반영
  - 중복 ID처럼 결과가 애매한 경우에만 mutation batch 종료 시 한 번 전체 재색인
- DOM tag/class candidate index를 추가하고 subtree 및 class mutation에 대해 증분 갱신
  - 후보 인덱스로 selector match 대상을 줄이되 실제 tree walk는 유지해 document order를 보존
  - 존재하지 않는 tag/class/id 후보는 tree walk 없이 즉시 빈 결과 반환
  - `className`, `classList`, class attribute 추가·교체·삭제를 전체 재색인 없이 반영
  - native `Node` API를 통한 직접 class 변경도 owning document의 후보 인덱스에 즉시 반영
- CSS cascade에서 후보 규칙의 selector match를 한 번만 수행하고 custom property와 일반 declaration 처리에 재사용
- live region 목록을 별도 유지하고 변경된 subtree와 관련된 live region만 `InnerText()` 재계산
  - 일반 style/scroll mutation에서는 `[aria-live]` 전체 검색을 수행하지 않음
  - `aria-live` attribute 추가·삭제를 별도로 추적
- `querySelector()` 전용 조기 종료 순회 추가
- query selector group을 tree node마다 다시 파싱하지 않고 query당 한 번만 compile
- lowercase attribute 조회의 임시 문자열 생성을 제거하고 mixed-case 입력에만 정규화 적용
- `HasClass()`와 CSS class candidate 수집에서 `wistringstream` 제거
- `InnerText()`를 단일 append buffer로 생성
- inline event handler를 event/source 기준으로 compile cache
- DOM child 교체 시 제거된 child의 parent link 정리

### 추가된 회귀 검증

- scoped/group selector 결과와 document-order 확인
- mixed-case attribute 및 class token scan 확인
- scroll/style/layout/tree mutation 단계와 batch 승격 확인
- `aria-live` membership mutation 확인
- 고유 subtree 삽입, ID 변경, subtree 삭제가 전체 재색인을 유발하지 않는지 확인
- 중복 ID mutation batch가 전체 재색인을 정확히 한 번 수행하는지 확인
- tag/class subtree 삽입·삭제와 class mutation이 전체 재색인 없이 query 결과에 반영되는지 확인
- 중복 ID fallback과 native class mutation이 document order와 query 정확성을 보존하는지 확인
- paint-only scroll이 전체 layout 없이 기존 child geometry를 정확히 한 번 이동시키는지 확인

### 다음 단계

- 동일 하드웨어에서 반복 계측 자동 비교
- selector structural revision 및 match cache
- dirty subtree geometry layout

### 적용하지 않을 최적화

- scroll bitmap을 이동하고 새로 노출된 영역만 다시 그리는 방식은 적용하지 않음
